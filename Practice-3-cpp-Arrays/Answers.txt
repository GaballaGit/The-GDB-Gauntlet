1. p/d numbers

Print out the elements in numbers as base 10 integers





















































2. p/a &numbers

p/a: a outputs in address form. use & in &numbers to retrieve the address of numbers.






























































3. x/20fg numbers (or) x/20fg &numbers

x/20fg: 20 outputs 20 elements, f for floats, g for giant words. We use float and giant because we are dealing with 64 bits.








































































4. p/u mixedNumbers

p/u: u is for unsigned integer (unsigned means only positive).